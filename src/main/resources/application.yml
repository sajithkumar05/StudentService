server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/student_info?createDatabaseIfNotExist=true
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true


